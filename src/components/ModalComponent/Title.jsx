import styled from "@emotion/styled";

const Title = styled.h1`
    font-weight: 700;
    text-align: center;
    margin: 16px 0;
    line-height: 48px;
    font-size: 32px;
`;

export default Title;
